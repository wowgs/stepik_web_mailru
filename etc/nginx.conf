server {
    error_log /var/log/nginx/myerrors.log info;
    listen 80;
    server_name _;

    location ^~ /uploads/ {
        root /home/box/web/;
    }

    location ~ ^.+\..+$ {
        root /home/box/web/public/;
    }

    location / {
        return 404;
    }
}
